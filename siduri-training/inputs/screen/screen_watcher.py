# TODO (later phase): detect scene/UI changes on screen (OCR, scene-detection,
# or OBS scene-switch hooks) and turn them into events.
#
# from core.events import Event
# event = Event(type="screen_change", payload={"message": "switched to donation alert overlay"})
# on_event(event)

# TODO (later phase): push short text summaries of what's happening in-game.
# Start manual/simple (you or a hotkey script sends events) before investing
# in game-API or vision-model integration.
#
# from core.events import Event
# event = Event(type="game_event", payload={"message": "boss fight started"})
# on_event(event)
